import { shortCodeConfig } from './types';
declare const _default: {
    initArguments: {};
    configFunction: (eleventyConfig: any, options: shortCodeConfig) => Promise<void>;
};
export default _default;
